/* Generated with TypeScript snippets */

import { ZipAsset } from "./ZipAsset";

/** AssetBundle **
* ...
* @Author Sith
* @Created 2020-11-09
*/

export class AssetBundle extends ZipAsset {

	/* constructor(id: string, source: { url: string } | { bundleID: string, entryID: number }) {
		super(id, source);
	} */

	//-------------------------------------------------- PUBLIC --------------------------------------------------

	/* public clear(): void {
		super.clear();
	} */

	//-------------------------------------------------- PRIVATE --------------------------------------------------

	/* protected init(): void {
		if (this._source.url)
			this._source.xhr = PIXI.LoaderResource.XHR_RESPONSE_TYPE.BUFFER;
	} */
}